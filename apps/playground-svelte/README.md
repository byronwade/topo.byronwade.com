# Svelte compatibility fixture

Executable SvelteKit project used to verify Topo file-route discovery, Vite runtime startup, browser rendering, flow projection, and shutdown. The same built-in adapter also supports routerless Svelte applications through `App.svelte`.
