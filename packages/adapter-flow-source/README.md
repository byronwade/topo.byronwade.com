# @topo/adapter-flow-source

Built-in static flow discovery for literal internal links, router navigation,
redirects, form submissions, and HTTP requests. Evidence is intentionally
conservative: computed destinations become issues and inferred journeys remain
read-only candidates until a developer records or accepts an authoritative
`.topo/flows/*.json` flow.
