# `@topo/adapter-nuxt`

Built-in Nuxt pages discovery for root, `src/pages`, `app/pages`, and layer page trees. It normalizes index, dynamic, optional, catch-all, route-group, client-only, and server-only conventions without executing Nuxt.
