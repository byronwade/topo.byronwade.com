# @topo/adapter-openapi

OpenAPI 3.x and Swagger 2 contract discovery. Topo reads conventional
`openapi*.json|yaml|yml` and `swagger*.json|yaml|yml` files from the shared
scanner snapshot and keeps malformed contracts visible as LLM-readable issues.
