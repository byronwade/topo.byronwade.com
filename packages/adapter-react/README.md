# `@topo/adapter-react`

Built-in route discovery for ordinary React applications. It reads static absolute React Router paths when they are present and otherwise exposes the concrete application entry as `/`. Computed and relative nested route declarations are intentionally not guessed.
