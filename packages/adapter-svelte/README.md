# `@topo/adapter-svelte`

Built-in discovery for Svelte and SvelteKit. SvelteKit `+page.svelte` and `+error.svelte` files become normalized screen states; a routerless Svelte application exposes its concrete `App.svelte` entry as `/`.
