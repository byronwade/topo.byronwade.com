# `@topo/adapter-vue`

Built-in route discovery for ordinary Vue applications. It reads static absolute Vue Router paths and otherwise exposes the concrete Vue application entry as `/`. Computed routes and relative nested children are left as explicit coverage gaps.
