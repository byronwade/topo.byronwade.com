# `@topo/docs-governance`

One deep module for Topo's documentation lifecycle. It validates the product manifest, feature evidence, documentation frontmatter, change fingerprints, generated changelog, public-site routes, and local Markdown links through one `validateDocumentation()` interface.
