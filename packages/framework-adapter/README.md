# `@topo/framework-adapter`

Stable framework discovery contract and deterministic adapter registry. Framework packages accept a read-only workspace snapshot and return normalized routes without depending on the scanner, daemon, CLI, or Studio.
