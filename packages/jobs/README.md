# `@topo/jobs`

Small durable serial job queue for scans, captures, probes, and exports. Job lifecycle records are stored through `@topo/storage`, so the Studio and CLI can report actual work state after a restart.
